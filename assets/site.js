document.addEventListener('DOMContentLoaded',()=>{const cards=[...document.querySelectorAll('.project-card')];if('IntersectionObserver'in window){const io=new IntersectionObserver(es=>{es.forEach(e=>{if(e.isIntersecting){e.target.classList.add('is-visible');io.unobserve(e.target)}})},{threshold:.12});cards.forEach(c=>{c.classList.add('reveal-card');io.observe(c)})}else{cards.forEach(c=>c.classList.add('is-visible'))}const buttons=[...document.querySelectorAll('[data-lightbox]')];if(!buttons.length)return;const box=document.createElement('div');box.className='bio-lightbox';box.setAttribute('role','dialog');box.setAttribute('aria-modal','true');box.innerHTML='<button class="bio-lightbox__close" type="button" aria-label="Close image">×</button><img alt="">';document.body.appendChild(box);const img=box.querySelector('img');const close=()=>{box.classList.remove('is-open');document.body.style.overflow=''};buttons.forEach(b=>b.addEventListener('click',()=>{const source=b.querySelector('img');img.src=source.src;img.alt=source.alt;box.classList.add('is-open');document.body.style.overflow='hidden';box.querySelector('button').focus()}));box.addEventListener('click',e=>{if(e.target===box||e.target.closest('.bio-lightbox__close'))close()});document.addEventListener('keydown',e=>{if(e.key==='Escape')close()})});